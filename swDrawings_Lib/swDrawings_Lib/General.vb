Public Class General
    Inherits Variables

   

    'Public Function GetBox_fc(ByVal swModel As SldWorks.SldWorks) As Object
    '    Dim swAssy As SldWorks.AssemblyDoc
    '    Dim swPart As SldWorks.PartDoc
    '    Dim vBox As Object

    '    If LCase(Right(swModel.GetPathName, 6)) = "sldasm" Then
    '        swAssy = swModel
    '        vBox = swAssy.GetBox(True)
    '    ElseIf LCase(Right(swModel.GetPathName, 6)) = "sldprt" Then
    '        swPart = swModel
    '        vBox = swPart.GetPartBox(True)
    '    Else
    '        vBox = Nothing
    '    End If

    '    Return vBox

    'End Function

    

End Class
